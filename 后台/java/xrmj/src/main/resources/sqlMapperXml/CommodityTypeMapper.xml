<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangzhixuan.mapper.CommodityTypeMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="CommodityType" type="com.wangzhixuan.model.CommodityType">
		<result column="id" property="id" />
		<result column="type_name" property="typeName" />
		<result column="rank" property="rank" />
		<result column="status" property="status" />
		<result column="user_id" property="userId" />
		<result column="create_time" property="createTime" />
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="Base_Column_List">
	    <![CDATA[
        id, type_name, rank, status, user_id, create_time 
	    ]]>
	</sql>
	
	<select id="selectCommodityTypePage"  resultMap="CommodityType">
		SELECT <include refid="Base_Column_List"/> 
		FROM commodity_type 
		<where>
			status = 0 
			<if test=" typeName != null and typeName != '' ">
	               and type_name like concat(concat('%',#{typeName}),'%') 
	            </if>
		</where>
		order by rank
    </select>
    
    <select id="getAllTyep"  resultMap="CommodityType">
		SELECT <include refid="Base_Column_List"/> 
		FROM commodity_type 
		where status = 0 
		order by rank
    </select>
    

</mapper>
