<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:tx="http://www.springframework.org/schema/tx" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:gorm="http://grails.org/schema/gorm"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation=
	   "http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/lang 
	    http://www.springframework.org/schema/lang/spring-lang-3.1.xsd
		http://www.springframework.org/schema/tx
		http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context-2.5.xsd
		http://www.springframework.org/schema/data/mongo
        http://www.springframework.org/schema/data/mongo/spring-mongo-1.0.xsd
		http://grails.org/schema/gorm http://grails.org/schema/gorm/gorm.xsd
		">
	<!-- 加载其他配置文件 -->
	<bean id="propertyConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations">
			<list>
				<value>config/properties/settings.properties</value>
			</list>
		</property>
	</bean>

	<!-- socket服务启动类 -->
	<bean id="serverInitializer" class="com.guse.stock.netty.SocketServer">
		<!-- 启动端口 -->
		<property name="port" value="${netty.port}" />
	</bean>
	<!-- http服务启动类 -->
	<bean id="httpServerInitializer" class="com.guse.stock.netty.HttpServer">
		<!-- 启动端口 -->
		<property name="port" value="${netty.http.port}" />
	</bean>
	<!-- http服务启动类 -->
	<bean id="wsServerInitializer" class="com.guse.stock.netty.WebSocketServer">
		<!-- 启动端口 -->
		<property name="port" value="${netty.websocket.port}" />
	</bean>
	
	<!-- 日志信息加载 -->
	<bean id="log4jInitialization" class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
		<property name="targetClass" value="org.springframework.util.Log4jConfigurer" />
		<property name="targetMethod" value="initLogging" />
		<property name="arguments">
			<list>
				<value>config/log4j.xml</value>
			</list>
		</property>
	</bean>
	
	<!-- MongoDB 连接信息注入 -->
	<bean id="mongoConnectUtil" class="com.guse.stock.mongo.MongoConnectUtil">
		<constructor-arg name="host" value="${mongo.host}" />
		<constructor-arg name="port" value="${mongo.port}" />
		<constructor-arg name="dbname" value="${mongo.dbname}" />
		<constructor-arg name="username" value="${mongo.username}" />
		<constructor-arg name="password" value="${mongo.password}" />
	</bean>

	<!-- MySQL配置信息 -->
	<bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="com.mysql.jdbc.Driver" />
		<property name="url"
			value="jdbc:mysql://${mysql.address}:${mysql.port}/${mysql.database}?zeroDateTimeBehavior=convertToNull&amp;transformedBitIsBoolean=true&amp;autoReconnect=true&amp;useUnicode=true&amp;characterEncoding=UTF-8" />
		<property name="username" value="${mysql.user}" />
		<property name="password" value="${mysql.password}" />
	</bean>
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<!-- <property name="configLocation" value="config/mybatis-config.xml" /> -->
	</bean>

	<!-- 注册spring上下文环境 -->
	<bean id="context" class="com.guse.stock.base.SpringContext" />
	
	<!-- 自动扫描该包下所有组件 -->
	<context:component-scan base-package="com.guse.stock" />

	<!-- 自动扫描所有dao并注入到spring容器 -->
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.guse.stock.dao" />
	</bean>

	<!-- 事物 -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>
	<tx:annotation-driven transaction-manager="transactionManager" />

</beans>